package com.zealthy.custom_onboarding.repository;

import com.zealthy.custom_onboarding.model.AdminConfig;

// Handles database operations for admin config entity
public interface AdminRepository extends CrudRepository<AdminConfig> {
}
